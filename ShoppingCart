using namespace std;
using std::endl;
using std::cin;
using std::cout;

class ShoppingCart{ 
private:
    int CartID;
    int UserID;
    int ISBN;
    int Quantity;

public:
    ShoppingCart();
    void getUserID();
    int getQuantity();
    int addItem(ISBN);
    int removeItem(ISBN);
    double totalCost();
    void viewCart();
    int changeQuantity();
    void checkout();
}
